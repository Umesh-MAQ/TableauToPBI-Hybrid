# Python
__pycache__/
*.pyc

# Power BI local cache / settings (never commit)
**/.pbi/localSettings.json
**/.pbi/cache.abf

# Generated migration outputs (the whole Output/ tree) are produced by the
# pipeline and are not version-controlled -- keep the repo small and free of
# regenerable artifacts. Run `migrate.py run "<data dir>"` to regenerate them.
Output/
